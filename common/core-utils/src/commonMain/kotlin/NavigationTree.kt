object NavigationTree {

    enum class Splash {
        SplashScreen
    }

    enum class Auth {
        AuthFlow, Login, Register, Forgot
    }

    object Main {
        enum class Mobile {
            Dashboard, Home, Search, Events, Streams
        }
    }
}